# Get all environment variables
System.get_env()
# Get just one:
System.get_env("USER")

# File extension
Path.extname("mods_funcs7.exs")

# Current working directory
System.cwd()

# Run shell command
System.cmd("ls", [])
